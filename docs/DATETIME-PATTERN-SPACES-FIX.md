# Datetime Pattern with Spaces - テスト手順

## 🎯 問題

```sql
@column 作成日時 format=datetime pattern=yyyy-MM-dd HH:mm:ss
```

このように空白を含むパターンを指定すると、`yyyy-MM-dd` までしか認識されない問題がありました。

## ✅ 修正内容

### Before（修正前）

```typescript
// 正規表現が空白で区切ってしまう
const optionMatches = optionsStr.matchAll(/(\w+)=([^\s]+)/g);
```

結果：
- `pattern=yyyy-MM-dd` → ✅ 認識
- `HH:mm:ss` → ❌ 無視される

### After（修正後）

```typescript
// ダブルクォート・シングルクォートをサポート
const optionMatches = optionsStr.matchAll(/(\w+)=("([^"]*)"|'([^']*)'|([^\s]+))/g);
```

結果：
- `pattern="yyyy-MM-dd HH:mm:ss"` → ✅ 完全に認識

## 📝 使い方

### パターン1：スペースなし（従来通り）

```sql
/**
 * @column 更新日時 format=datetime pattern=yyyy/MM/dd_HH:mm:ss
 */
SELECT 更新日時 FROM users;
```

結果：`2025/12/28_14:30:00`

### パターン2：スペースあり（ダブルクォート）

```sql
/**
 * @column 作成日時 format=datetime pattern="yyyy-MM-dd HH:mm:ss"
 */
SELECT 作成日時 FROM users;
```

結果：`2025-12-28 14:30:00`

### パターン3：スペースあり（シングルクォート）

```sql
/**
 * @column 登録日時 format=datetime pattern='yyyy/MM/dd HH:mm:ss'
 */
SELECT 登録日時 FROM users;
```

結果：`2025/12/28 14:30:00`

### パターン4：複数オプションと組み合わせ

```sql
/**
 * @column ID align=right
 * @column 金額 align=right format=number comma=true
 * @column 作成日時 format=datetime pattern="yyyy-MM-dd HH:mm:ss"
 * @column 更新日時 format=datetime pattern="yyyy/MM/dd HH:mm"
 */
SELECT ID, 金額, 作成日時, 更新日時 FROM orders;
```

## 🧪 テストケース

### Test 1: スペース込みパターン（ダブルクォート）

**入力:**
```sql
/**
 * @column created_at format=datetime pattern="yyyy-MM-dd HH:mm:ss"
 */
SELECT '2025-12-28T14:30:45' AS created_at;
```

**期待される出力:**
```
2025-12-28 14:30:45
```

### Test 2: スペース込みパターン（シングルクォート）

**入力:**
```sql
/**
 * @column updated_at format=datetime pattern='yyyy/MM/dd HH:mm:ss'
 */
SELECT NOW() AS updated_at;
```

**期待される出力:**
```
2025/12/28 14:30:45
```

### Test 3: アンダースコア区切り（クォートなし）

**入力:**
```sql
/**
 * @column timestamp format=datetime pattern=yyyy-MM-dd_HH:mm:ss
 */
SELECT NOW() AS timestamp;
```

**期待される出力:**
```
2025-12-28_14:30:45
```

### Test 4: 複数カラムの混在

**入力:**
```sql
/**
 * @column id align=right
 * @column name width=200px
 * @column amount type=int align=right format=number comma=true
 * @column created format=datetime pattern="yyyy-MM-dd HH:mm:ss"
 * @column modified format=datetime pattern=yyyy/MM/dd_HH:mm
 */
SELECT 
    1 AS id,
    'Test' AS name,
    1234567 AS amount,
    NOW() AS created,
    NOW() AS modified;
```

**期待される出力:**
```
id: 1 (右寄せ)
name: Test (幅200px)
amount: 1,234,567 (右寄せ、カンマ区切り)
created: 2025-12-28 14:30:45 (スペース区切り)
modified: 2025/12/28_14:30 (アンダースコア区切り)
```

## 🔍 実装詳細

### 正規表現の説明

```typescript
/(\w+)=("([^"]*)"|'([^']*)'|([^\s]+))/g
```

#### 分解：

1. `(\w+)` - キー名（英数字とアンダースコア）
2. `=` - 等号
3. `("([^"]*)"|'([^']*)'|([^\s]+))` - 値（3パターン）
   - `"([^"]*)"` - ダブルクォートで囲まれた値
   - `'([^']*)'` - シングルクォートで囲まれた値
   - `([^\s]+)` - 通常の値（空白まで）

#### マッチグループ：

- `optionMatch[1]` - キー名
- `optionMatch[2]` - 値全体（クォート含む）
- `optionMatch[3]` - ダブルクォート内の値
- `optionMatch[4]` - シングルクォート内の値
- `optionMatch[5]` - 通常の値

#### 値の取得：

```typescript
const value = optionMatch[3] || optionMatch[4] || optionMatch[5];
```

クォートで囲まれている場合は自動的にクォートが除去されます。

## 📚 対応するフォーマット文字

| 記号 | 意味 | 例 |
|------|------|-----|
| `yyyy` | 4桁年 | 2025 |
| `yy` | 2桁年 | 25 |
| `MM` | 2桁月 | 12 |
| `M` | 月（0埋めなし） | 12 |
| `dd` | 2桁日 | 28 |
| `d` | 日（0埋めなし） | 28 |
| `HH` | 2桁時（24時間） | 14 |
| `H` | 時（0埋めなし） | 14 |
| `mm` | 2桁分 | 30 |
| `m` | 分（0埋めなし） | 30 |
| `ss` | 2桁秒 | 45 |
| `s` | 秒（0埋めなし） | 45 |

**Note:** 現在の実装では `yyyy`, `MM`, `dd`, `HH`, `mm`, `ss` のみサポート

## 🎨 よく使うパターン集

### 日本の標準形式

```sql
-- 2025年12月28日 14:30:45
pattern="yyyy年MM月dd日 HH:mm:ss"

-- 2025/12/28 14:30
pattern="yyyy/MM/dd HH:mm"

-- 2025-12-28 14:30
pattern="yyyy-MM-dd HH:mm"
```

### ISO 8601形式

```sql
-- 2025-12-28T14:30:45
pattern="yyyy-MM-ddTHH:mm:ss"

-- 2025-12-28 14:30:45Z
pattern="yyyy-MM-dd HH:mm:ssZ"
```

### ファイル名用（スペースなし）

```sql
-- 20251228_143045
pattern=yyyyMMdd_HHmmss

-- 2025-12-28_14-30-45
pattern=yyyy-MM-dd_HH-mm-ss
```

### ログ用

```sql
-- [2025-12-28 14:30:45]
pattern="[yyyy-MM-dd HH:mm:ss]"

-- 2025/12/28 14:30:45.000
pattern="yyyy/MM/dd HH:mm:ss"
```

## ✅ 互換性

### ✅ 動作する形式

```sql
pattern=yyyy/MM/dd              -- スペースなし
pattern=yyyy-MM-dd              -- スペースなし
pattern="yyyy-MM-dd HH:mm:ss"   -- ダブルクォート
pattern='yyyy-MM-dd HH:mm:ss'   -- シングルクォート
pattern="yyyy年MM月dd日"         -- 日本語含む
```

### ❌ 動作しない形式（修正前）

```sql
pattern=yyyy-MM-dd HH:mm:ss     -- スペースあり、クォートなし → HH:mm:ssが無視される
```

## 🚀 まとめ

**修正により、以下が可能になりました：**

1. ✅ スペースを含むdatetimeパターンをダブルクォートで指定
2. ✅ シングルクォートもサポート
3. ✅ 従来の書き方（クォートなし）も引き続き動作
4. ✅ 複数のオプションとの組み合わせも正常動作

**推奨される書き方：**

```sql
-- スペースが含まれる場合はダブルクォート
pattern="yyyy-MM-dd HH:mm:ss"

-- スペースが含まれない場合はクォート不要
pattern=yyyy/MM/dd_HH:mm:ss
```

---

**修正日:** 2025-12-28  
**バージョン:** 0.1.3+  
**修正ファイル:** `src/sqlCommentParser.ts`

